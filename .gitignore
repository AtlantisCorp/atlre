# Excludes the builds directory.
build/*

# Excludes the docs directory.
docs/*

# Excludes the libs directory: libs are submodules and must be 
# inited with git submodules init.
libs/*

# Excludes all .DS_Store files.
*.DS_Store